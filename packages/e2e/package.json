{
  "name": "@pulsedock/e2e",
  "version": "0.6.0",
  "private": true,
  "description": "Playwright E2E tests for PulseDock",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  },
  "engines": {
    "node": ">=18"
  }
}
