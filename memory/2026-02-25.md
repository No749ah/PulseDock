2026-02-25 Heartbeat & Actions

- Timestamp: 2026-02-25 10:51 UTC
- Actions performed today (summary):
  - Mobile navbar toggle added; responsive UI quick wins applied (app-frame.tsx).
  - Cleaned build artifacts (.next, dist) and updated .gitignore.
  - Standardized env handling: API_PORT=4321, WEB_PORT=1234; created per-app .env.local files.
  - Added start/stop/restart scripts and repo-root npm wrappers: npm run start:web / stop:web / start:api / stop:api.
  - Implemented socket.io-based control wrappers (server.js/server.stop.js) for cross-OS npm stop/start.
  - Generated SSH deploy key at projects/PulseDock/.keys/id_ed25519_deploy (public key provided) and used it to push dev.
  - Cleaned and consolidated logs into ./logs and projects/PulseDock/logs (ignored in git).
  - Replaced old landing hero media with a modern, lightweight landing; iterated to an animated, info-forward landing.
  - Built web successfully and started services (web on 1234, api on 4321).
  - Created helper scripts: scripts/run-in-project.sh to load env files for commands.
  - Updated START.md with canonical root commands and troubleshooting.

- User preferences & conventions captured:
  - Always use repo root commands to control services: "npm run start:web" and "npm run stop:web" (also start:api/stop:api). Assistant must use these going forward.
  - Preferred ports: WEB_PORT=1234, API_PORT=4321.
  - Deploy key stored at projects/PulseDock/.keys/id_ed25519_deploy (private in workspace, public added to GitHub). Key location ignored by git.
  - Landing page should be modern, media-light, and AI-enhanced; assistant to iterate using modern stacks (Next.js + Tailwind/shadcn/ui) and add AI hooks.

- Branches / commits of note:
  - heartbeat/env-update (local work); merged into dev and pushed.
  - feature/landing-ai planned for the new landing work.

- Operational notes:
  - Use scripts/start-web.sh and scripts/start-api.sh for consistent starts; wrappers compute REPO_ROOT and set envs.
  - Stop scripts try socket-based graceful stop then may fall back to killing matching node processes.

- Next recommended actions (from assistant):
  - Confirm whether to proceed with quick modern landing (no Tailwind install) or full integration (install Tailwind + shadcn/ui and replace landing). User chose to proceed with modern landing iterations.
  - Consider adding systemd/docker-compose service entries for persistence.


