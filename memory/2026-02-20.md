

## Later Work (same day)

### Git / Branch Hygiene
- Merged all `heartbeat/*` branches into `dev` locally; resolved multiple merge conflicts (notably `apps/web/components/app-frame.tsx`, `apps/web/app/admin/page.tsx`, `apps/web/app/projects/page.tsx`, `apps/web/app/globals.css`, `alerts/monitors` wrap changes).
- Created a new SSH deploy key on host and had user add it to GitHub with write access.
- Pushed `dev` to GitHub (`origin/dev`) and deleted all remote `origin/heartbeat/*` branches.
- Configured git author identity for commits as:
  - name: `no749ah`
  - email: `no749ah@users.noreply.github.com`

### Runtime / Port Contract
- Restarted latest stack on canonical dev ports:
  - Web: http://localhost:4000
  - API: http://localhost:4001
  - Verified: `/health` on API is 200 and `/api/health` via Next rewrite is 200.

### Login HTML error fix (proxy rewrite)
- Root cause: `apps/web/next.config.mjs` had lost the `/api/:path* -> http://localhost:4001/:path*` rewrite, causing Next to return HTML 404 pages for API calls (login saw HTML instead of JSON).
- Restored rewrites and ensured web dev runs on :4000.

### HMR crash fix (Mantine Avatar module factory deleted)
- Root cause: Turbopack HMR instability ("module factory is not available; deleted in an HMR update") involving Mantine `Avatar` imported via `user-profile.tsx`.
- Mitigation: forced dev server to use webpack:
  - `apps/web/package.json` dev script: `next dev --webpack -p 4000`

### Landing page de-cramp
- Reduced “duplicate/too cramped” marquee behavior:
  - Render marquee track once (not doubled)
  - On mobile: disable marquee animation; wrap badges to avoid cramped duplication.

### Status page editor: real Drag & Drop builder
- Installed `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` in web.
- Replaced `/status/pages/[id]/edit` JSON-first editor with a block-based editor:
  - Left: block library (Header / Monitors / Versions)
  - Middle: canvas with sortable DnD ordering
  - Right: block settings (title, enabled, props JSON for now)
  - Top: calm status line (Draft/Published + saved time)
- Fixed `Cannot GET /v1/status-pages/undefined` by making `page.tsx` a server wrapper that passes `params.id` to a client component.

### Recent commits (dev)
- `2f328aa` force webpack dev server
- `0aa0bcd` add DnD block editor + fix edit id param
- plus earlier merge-repair commit `503ed76` (status-pages controller cleanup)
