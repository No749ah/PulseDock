# 2026-02-19 Session Notes — PulseDock

## Session Summary
Completed two heartbeat cycles for PulseDock monitoring platform. Fixed critical frontend hydration issues and improved responsive UI.

## Work Completed

### Heartbeat 1 (15:26 UTC)
- **Mobile UI Improvement**: Added `Table.ScrollContainer` wrapper to dashboard "Latest runs" table with `minWidth={720}` to prevent overflow on mobile
- **Branch**: `heartbeat/20260219-152603`
- **Commit**: Mobile responsive table scrolling on dashboard page

### Heartbeat 2 (15:32 UTC)
- **Responsive Navbar**: Added Mantine `Burger` component + `useDisclosure` hook to `app-frame.tsx`
  - Mobile hamburger toggle for sidebar nav
  - Auto-collapse navbar on small screens
  - Auto-close on nav item click
  - Hid long user label on mobile (`visibleFrom="sm"`)
- **Branch**: `heartbeat/20260219-153215`
- **Commit**: Mobile navbar improvements

### Bug Fixes (15:52 UTC)
- **Hydration Mismatch**: Added `suppressHydrationWarning` to login form + card
  - Root cause: URL params read via `useEffect` cause server/client render diff
  - Solution prevents React warning without breaking functionality
- **TypeScript Error**: Fixed `user-profile.tsx` — removed invalid `import type { ReturnType } from 'typescript'`
  - `ReturnType` is built-in utility type, not exported
- **Localization**: Changed German label "Benutzer merken" → English "Remember this account"
- **Branch**: `heartbeat/20260219-153215`
- **Commit**: Fixed hydration errors and build issues

## Runtime Status
✅ API: Running on :4000 (NestJS, health check OK)
✅ Web: Running on :3000 (Next.js 16.1.6, builds successful)
✅ DB: PostgreSQL + Redis healthy
✅ Both services passing endpoint health checks

## API Connectivity
- Local dev: Frontend uses `http://localhost:4000`
- Remote test: Frontend on `oc-web-test.no749ah.com` uses `https://oc-api-test.no749ah.com`
- Connection routing is smart and environment-aware

## Top Gaps Identified
1. **Mobile Data Tables**: Monitor/Alerts/Versions pages still use desktop-style tables (need same scroll wrapper pattern)
2. **Responsive Testing**: No Playwright mobile breakpoint coverage — regressions possible
3. **Product Docs**: README lacks differentiation vs competitors and roadmap clarity

## Next Priority Actions
1. Apply mobile table pattern across monitors/alerts/versions pages (highest impact)
2. Add Playwright E2E tests for mobile nav + dashboard
3. Expand README with competitive positioning and feature roadmap

## Key Learnings
- Mantine components (`Burger`, `useDisclosure`, `Table.ScrollContainer`) simplify mobile UX
- Hydration mismatches happen when useEffect reads client-only data (URL params, Math.random, etc.)
- TypeScript built-in utils (ReturnType, Omit, etc.) don't need imports — use directly
- Both services auto-restarted smoothly; port conflicts resolved by killing stale processes

## Files Modified This Session
- `apps/web/components/app-frame.tsx` (mobile nav)
- `apps/web/app/dashboard/page.tsx` (responsive table)
- `apps/web/app/login/page.tsx` (hydration fix)
- `apps/web/components/user-profile.tsx` (TypeScript fix)

## Git Branches
- `heartbeat/20260219-152603` → mobile table scrolling
- `heartbeat/20260219-153215` → mobile nav + bug fixes
- Both pushed to origin, PR links available

## Preference
- User preference: Always restart the PulseDock project services after finishing work so the latest version is running.

## Late Session Addendum (23:00–23:55 UTC)
### Stability / Proxy / SSR fixes
- Fixed SSR relative fetch crashes by making `API_BASE` SSR-safe:
  - Browser uses `NEXT_PUBLIC_API_BASE_URL` defaulting to `/api`
  - Server/SSR uses absolute base (`API_INTERNAL_BASE_URL` or absolute `NEXT_PUBLIC_API_BASE_URL`, fallback `http://localhost:4001`)
  - File: `apps/web/lib/api.ts`
- Hardened public status rendering to avoid Next 500s when public API fails:
  - File: `apps/web/app/status/[userId]/page.tsx`

### Same-origin API proxy in Next
- Added Next `rewrites()` so `/api/*` proxies to `http://localhost:4001/*` (works for dev + `next start` as long as API is localhost-reachable):
  - File: `apps/web/next.config.mjs`

### Mobile sidebar behavior
- Mobile sidebar now closes after nav clicks / route changes (Burger toggle + collapsed navbar):
  - File: `apps/web/components/app-frame.tsx`

### Client error visibility without DevTools
- Added Next error boundaries (`app/error.tsx`, `app/global-error.tsx`) with visible Error ID + friendly recovery UI.
- Added browser error capture into `localStorage` (`pulsedock_client_errors`) via `window.onerror` + `unhandledrejection`:
  - File: `apps/web/app/providers.tsx`

### Login robustness
- Made `/login` dynamic (`force-dynamic`, `revalidate=0`) to reduce stale HTML/chunk mismatch issues behind CDN.
- Improved login error message: detect HTML error pages and show a clear message instead of dumping HTML.
- Refactored login into server wrapper + client component to satisfy Next build rules:
  - Files: `apps/web/app/login/page.tsx`, `apps/web/app/login/login-client.tsx`

### Navigation completeness
- Added missing links to nav:
  - `Alarm Settings` → `/alarms`
  - `Status Page Builder` → `/status/builder` (adminOnly)

### Ops notes
- Git push failed due to missing SSH key (`Permission denied (publickey)`), but commits were created locally.
- Multiple restarts performed: stop processes on :4000/:4001, rebuild, restart Next on :4000 and API on :4001, then health-check `/health` and `/api/health`.
