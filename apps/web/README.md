# @pulsedock/web

Next.js frontend for PulseDock.

## Dev

```bash
npm run dev:web
```

Runs on `:1234`. Proxies `/api/*` to `:4321`.

## Build

```bash
npm run build -w @pulsedock/web
```

## Start (production)

```bash
npm run start:web
```

Uses `next start` — serves static assets natively.
