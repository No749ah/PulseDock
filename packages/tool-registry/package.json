{
  "name": "@pulsedock/tool-registry",
  "version": "1.5.0",
  "private": true,
  "description": "PulseDock tool registry — versioned tool templates for version monitoring",
  "main": "src/index.ts",
  "scripts": {
    "lint": "npx ts-node --project tsconfig.json scripts/lint-registry.ts",
    "audit:variants": "npx ts-node --project tsconfig.json scripts/variant-audit.ts"
  },
  "license": "Apache-2.0"
}
