{
  "name": "@pulsedock/extension",
  "version": "0.1.0",
  "description": "PulseDock browser extension — one-click monitor creation from any tab",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/post-build.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
